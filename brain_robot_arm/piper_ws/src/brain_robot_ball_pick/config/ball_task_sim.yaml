visual_search_controller:
  ros__parameters:
    backend: simulation
    real_motion_enabled: false
    observe_pose_calibrated: true
    execute_prepare: true
    use_sim_time: true
    joint_names: [joint1, joint2, joint3, joint4, joint5, joint6]
    observe_joint_positions: [0.0, 0.98, -0.75, 0.0, 0.30, 0.0]
    horizontal_joint: joint1
    vertical_joint: joint5
    horizon_joint_names: [joint4, joint6]
    horizon_lock_positions: [0.0, 0.0]
    # Ball task deliberately skips the cup's J2/J3/J5 leveling sequence.
    direct_grasp_after_align: true
    level_align_enabled: false
    horizontal_error_sign: -1.0
    vertical_error_sign: 1.0
    horizontal_search_min: -0.98
    horizontal_search_max: 0.98
    vertical_search_min: -0.60
    vertical_search_max: 0.95
    local_horizontal_range: 0.08
    local_vertical_range: 0.12
    horizontal_search_speed: 0.12
    vertical_search_speed: 0.15
    joint_position_tolerance: 0.01
    search_timeout_s: 75.0
    local_search_timeout_s: 5.0
    target_timeout_s: 0.60
    joint_state_timeout_s: 0.50
    target_acquire_frames: 1
    align_stable_frames: 5
    target_acquire_error_ratio: 0.20
    horizontal_kp: 0.0008
    vertical_kp: 0.0008
    align_joint_speed_limit: 0.08
    horizon_lock_kp: 1.0
    horizon_lock_speed_limit: 0.05
    horizon_lock_tolerance: 0.02
    control_rate_hz: 20.0
    prepare_velocity_scaling: 0.05
    prepare_acceleration_scaling: 0.05
    prepare_planning_time_s: 10.0
    error_topic: /brain_robot_vision/pixel_error
    target_valid_topic: /brain_robot_vision/target_valid
    joint_state_topic: /joint_states
    joint_command_topic: /servo_node/delta_joint_cmds
    servo_status_topic: /servo_node/status
    servo_start_service: /servo_node/start_servo
    servo_stop_service: /servo_node/stop_servo
    emergency_stop_topic: /brain_robot_control/emergency_stop
grasp_lift_executor:
  ros__parameters:
    simulation_only: true
    auto_execute: true
    prepare_on_grasp_ready: true
    # The PiPER cannot reach a vertical top-down ball grasp in this setup.
    # Keep the visual-aligned tool pose and approach from the upper side instead.
    top_down_grasp_enabled: false
    diagonal_side_grasp_enabled: true
    # Do not attach the Gazebo ball until it is actually between the two jaws.
    grasp_contact_check_enabled: true
    grasp_center_offset_m: 0.1358
    grasp_contact_tolerance_m: 0.025
    use_sim_time: true
    arm_group: arm
    gripper_group: gripper
    end_effector_link: gripper_base
    cup_name: red_ball
    open_target: open
    close_target: close
    lift_reference_frame: world
    camera_optical_frame: wrist_camera_optical_frame
    target_point_topic: /brain_robot_vision/target_point_camera
    target_timeout_s: 0.60
    grasp_height_offset_m: 0.0
    gripper_tip_offset_m: 0.1358
    top_down_pregrasp_clearance_m: 0.12
    top_down_grasp_clearance_m: 0.005
    top_down_grasp_yaw: 0.0
    # Same downward tool axis, alternate yaw only when a PiPER joint solution
    # is unavailable at the first orientation.
    top_down_yaw_candidates: [0.0, 1.57079632679, -1.57079632679, 3.14159265359]
    # Pregrasp is behind the ball along the wrist-camera optical axis and above it.
    # The final motion keeps that pose and follows the same slanted camera/jaw axis.
    diagonal_pregrasp_standoff_m: 0.17
    diagonal_grasp_standoff_m: 0.045
    diagonal_pregrasp_lift_m: 0.10
    diagonal_grasp_lift_m: 0.04
    # Retained for the shared executor's side-grasp validation; unused here.
    pregrasp_standoff_m: 0.12
    grasp_depth_m: 0.045
    cartesian_eef_step_m: 0.003
    linear_approach_velocity_scaling: 0.02
    lift_distance_m: 0.08
    grasp_planning_time_s: 8.0
    grasp_planning_attempts: 10
    grasp_velocity_scaling: 0.05
    grasp_acceleration_scaling: 0.05
    grasp_position_tolerance: 0.008
    grasp_orientation_tolerance: 0.05
    grasp_scene_wait_s: 10.0
    visual_state_topic: /brain_robot_visual_control/state
    grasp_scene_ready_topic: /brain_robot_ball_pick/scene_ready
    grasp_state_topic: /brain_robot_grasp/state
    emergency_stop_topic: /brain_robot_control/emergency_stop
    cup_follow_service: /brain_robot_ball_pick/set_ball_follow
    servo_stop_service: /servo_node/stop_servo
